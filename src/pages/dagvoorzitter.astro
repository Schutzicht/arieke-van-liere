---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import Button from '../components/ui/Button.astro';
import BookingCTA from '../components/BookingCTA.astro';
---

<Layout title="Arieke van Liere - Dagvoorzitter">
    <Header />
    
    <main>
        <!-- Service Hero -->
        <section class="relative pt-32 pb-20 bg-gray-900 text-white overflow-hidden min-h-[60vh] flex items-center">
            <div class="absolute inset-0 z-0">
                <img src="/assets/images/hero-arieke.jpg" alt="Arieke op stage" class="w-full h-full object-cover opacity-40 mix-blend-overlay" />
                <div class="absolute inset-0 bg-gradient-to-t from-gray-900 via-transparent to-transparent"></div>
            </div>
            
            <div class="container-custom relative z-10">
                <span class="text-brand-pink font-bold tracking-widest uppercase text-sm mb-4 block animate-fade-in">Core Service</span>
                <h1 class="text-5xl md:text-7xl font-display mb-6 max-w-3xl animate-slide-up">
                    Energie, scherpte en <span class="text-brand-green">verbinding</span> in de zaal.
                </h1>
                <p class="text-xl text-gray-300 max-w-2xl mb-8 leading-relaxed animate-slide-up" style="animation-delay: 0.1s;">
                    Als dagvoorzitter zorg ik dat jouw evenement staat als een huis. Ik voel de zaal aan, stel de juiste vragen en zorg dat niemand naar huis gaat zonder nieuwe inzichten.
                </p>
                <div class="flex flex-wrap gap-4 animate-slide-up" style="animation-delay: 0.2s;">
                    <Button href="#contact" variant="primary">Boek als Dagvoorzitter</Button>
                    <Button href="#werkwijze" variant="outline" class="border-white text-white hover:bg-white hover:text-gray-900">Mijn werkwijze</Button>
                </div>
            </div>
        </section>

        <!-- Waarom Arieke? -->
        <section class="py-24 bg-white" id="werkwijze">
            <div class="container-custom grid md:grid-cols-2 gap-16 items-center">
                <div class="relative">
                    <div class="bg-gray-100 rounded-[3rem] p-8 relative z-10 rotate-[-2deg]">
                        <img src="/assets/images/instagram/post1.jpg" alt="Arieke in actie" class="rounded-2xl shadow-xl w-full" />
                    </div>
                    <div class="absolute inset-0 bg-brand-pink/20 rounded-[3rem] rotate-[3deg] blur-xl -z-10"></div>
                </div>
                
                <div class="space-y-6">
                    <h2 class="text-4xl font-display mb-4">Geen standaard praatje.</h2>
                    <p class="text-gray-600 text-lg">
                        Een goede dagvoorzitter is meer dan iemand die de sprekers aankondigt. Het is de <span class="font-bold text-gray-900">rode draad</span> van je evenement.
                    </p>
                    <ul class="space-y-4 pt-4">
                        {[
                            "Scherpe interviews en panelgesprekken",
                            "Interactie met het publiek (geen saaie zenders!)",
                            "Flexibel en stressbestendig bij onverwachte wendingen",
                            "Humor en relativering waar het kan, serieus waar het moet"
                        ].map(item => (
                            <li class="flex items-center gap-3 text-gray-800 font-medium">
                                <span class="w-6 h-6 rounded-full bg-brand-green/20 text-brand-green flex items-center justify-center text-xs">✓</span>
                                {item}
                            </li>
                        ))}
                    </ul>
                </div>
            </div>
        </section>

        <BookingCTA />
    </main>
    
    <Footer />
</Layout>
